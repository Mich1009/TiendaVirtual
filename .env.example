# API
PORT=4000
API_BASE_URL=http://localhost:4000/v1

# Database
# Debe coincidir con el usuario y contraseña definidos en Docker Compose
# (ver infra/docker-compose.yml -> POSTGRES_USER/POSTGRES_PASSWORD)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/tiendavirtual

# Auth
JWT_SECRET=super-secret-demo-change-me

# Cloudinary (si se usa para imágenes)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=tiendavirtual/products

# SMTP (Gmail) para recuperación de contraseña
# 1) Activa 2FA en tu cuenta Google.
# 2) Genera un "App Password" (Código de aplicación) y úsalo en SMTP_PASS.
# 3) Coloca tu correo en SMTP_USER y opcionalmente define SMTP_FROM.
#
# Correo SMTP (por ejemplo, tu_correo@gmail.com)
SMTP_USER=
# App Password de Gmail (NO tu contraseña normal)
SMTP_PASS=
# Host y puerto de Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
# Remitente mostrado en el correo (opcional, por defecto SMTP_USER)
# Ejemplo: Tienda Virtual <tu_correo@gmail.com>
SMTP_FROM=